gcp:
  project_id: "gen-ai-guru-gdg-pune"
  region: "us"
  multi_region: "us"

ces:
  # Replace with your staging app ID when you have a separate staging app
  app_id: "e9c8da8d-c9fc-4ec4-aab0-4632406e8868"
  agent_id: ""
  api_endpoint: "ces.googleapis.com"
  oauth_scope: "https://www.googleapis.com/auth/ces"

storage:
  bucket_name: "gen-ai-guru-gdg-pune-agent-exports"
  export_prefix: "agent-exports/staging"
  backup_prefix: "agent-backups/staging"

deployment:
  environment: "staging"
  auto_rollback: true
  smoke_test_timeout: 120
  max_retries: 3

overrides:
  data_store_uris: {}
  service_endpoints: {}
  storage_buckets: {}
  custom_settings:
    log_level: "INFO"
    enable_debug_mode: false
